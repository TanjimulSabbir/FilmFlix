/* eslint-disable react/prop-types */
function Toggle({ item }) {
    return (
        <div>
            {/* <p key={item.title}
                className={`cursor-pointer py-[3px] px-6 text-base ${(slectToggle === item.title) ? style.Toggled : "text-black"}`} onClick={() => handleToggle(item)}>
                {item.title}</p> */}
        </div>
    )
}

export default Toggle;